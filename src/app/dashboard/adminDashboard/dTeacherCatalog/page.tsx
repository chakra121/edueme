import MainTeachersContainer from "./MainTeachersContainer";

export default function TeacherCatalogPage() {
  return <MainTeachersContainer />;
}
// This page is the main entry point for the teacher catalog in the admin dashboard.